# navigation

`RootNavigator` switches Auth vs App (bottom tabs) off `state.auth.token` and
applies the light/dark navigation theme. `navigationRef` lets non-UI code
navigate. Route names live in `routes.ts`, param lists in `types.ts`.
