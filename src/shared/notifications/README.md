# notifications

Push notification entry points live here so feature screens do not depend on a
specific provider. Install and wire Expo Notifications, Firebase Messaging, or a
custom provider behind these facades.
