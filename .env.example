# Public Expo env vars are exposed to the app at build time.
EXPO_PUBLIC_API_URL=https://api.example.com

# Set to false to use the offline __DEV__ mock instead of real hosted APIs.
EXPO_PUBLIC_USE_REAL_API=true
