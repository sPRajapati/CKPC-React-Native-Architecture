# permissions

Normalized permission helpers (`granted`/`denied`/`undetermined`/`unavailable`).
The native modules are optional — activate with:

    npx expo install expo-camera expo-location expo-notifications
